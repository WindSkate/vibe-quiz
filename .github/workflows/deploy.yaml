name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: | 
          cd ~/projects/vibe-quiz
          git pull origin main
          mkdir -p .data/postgres_data
          mkdir -p .data/storage
          docker compose up -d --build
